<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="30pt"
    android:paddingBottom="10pt"
    android:paddingLeft="10pt"
    android:paddingRight="10pt">

    <include layout="@layout/grid"/>

    <com.frontend.NumberSelector
        android:id="@+id/numberSelector"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp" />

    <!-- Undo | Undo all -->
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp">

        <com.frontend.ShinyButton
            android:id="@+id/undoButton"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:text="@string/undo_for_the_weak"
            android:textSize="11sp"
            android:maxLines="1"/>

        <com.frontend.ShinyButton
            android:id="@+id/undoUntilValidButton"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="@string/undo_all_for_the_bad"
            android:textSize="11sp"
            android:maxLines="1"/>

    </LinearLayout>

    <!-- Solve -->
    <com.frontend.ShinyButton
        android:id="@+id/solveButton"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="8dp"
        android:text="@string/solve_for_the_cheaters"
        android:textSize="11sp"
        android:maxLines="1"/>

    <!-- Empty -->
    <com.frontend.ShinyButton
        android:id="@+id/emptyButton"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="8dp"
        android:text="@string/empty"
        android:maxLines="1"/>

    <!-- Save row -->
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center_vertical">

        <EditText
            android:id="@+id/sudokuName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:inputType="text"
            android:hint="@string/sudoku_name"
            android:text=""/>

        <com.frontend.ShinyButton
            android:id="@+id/saveButton"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:layout_marginStart="8dp"
            android:text="@string/save"
            android:maxLines="1"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"/>

    </LinearLayout>

</LinearLayout>
